---
is_a: "[[software]]"
topics: "[[linux]]"
---
# Notes
Without filename
```
-h
--no-filename
```

Only showing filenames
```
grep -l
```

Lines that start with a number
`grep -h '^[0-9]' *`

regex grep
```
grep -E
```